services:
  - type: web
    name: videoia-backend
    runtime: node
    plan: free
    buildCommand: apt-get update && apt-get install -y ffmpeg && npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PUBLIC_URL
        value: https://videoia-backend.onrender.com
